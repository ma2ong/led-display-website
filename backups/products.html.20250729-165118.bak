<!DOCTYPE html>
{% extends "base.html" %}

{% block title %}Products - LED Display Admin{% endblock %}
{% block page_title %}Product Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Products</h2>
    <a href="{{ url_for('add_product') }}" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i>Add New Product
    </a>
</div>

<div class="card shadow">
    <div class="card-body">
        {% if products %}
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>ID</th>
                            <th>Product Name</th>
                            <th>Category</th>
                            <th>Status</th>
                            <th>Created</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for product in products %}
                        <tr>
                            <td>{{ product.id }}</td>
                            <td>
                                <div>
                                    <strong>{{ product.name_en }}</strong>
                                    <br>
                                    <small class="text-muted">{{ product.name_zh }}</small>
                                </div>
                            </td>
                            <td>
                                <span class="badge bg-secondary">{{ product.category }}</span>
                            </td>
                            <td>
                                {% if product.status == 'active' %}
                                    <span class="badge bg-success">Active</span>
                                {% else %}
                                    <span class="badge bg-warning">Inactive</span>
                                {% endif %}
                            </td>
                            <td>{{ product.created_at[:10] }}</td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a href="{{ url_for('api_products') }}" class="btn btn-sm btn-outline-info" title="View API">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <form method="POST" action="{{ url_for('delete_product', id=product.id) }}" 
                                          style="display: inline;" 
                                          onsubmit="return confirm('Are you sure you want to delete this product?')">
                                        <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% else %}
            <div class="text-center py-5">
                <i class="fas fa-tv fa-3x text-muted mb-3"></i>
                <h5 class="text-muted">No products found</h5>
                <p class="text-muted">Start by adding your first product.</p>
                <a href="{{ url_for('add_product') }}" class="btn btn-primary">
                    <i class="fas fa-plus me-2"></i>Add Product
                </a>
            </div>
        {% endif %}
    </div>
</div>

<!-- API Information -->
<div class="card shadow mt-4">
    <div class="card-header">
        <h6 class="m-0 font-weight-bold text-primary">API Information</h6>
    </div>
    <div class="card-body">
        <p>Products are automatically available via API for frontend synchronization:</p>
        <ul>
            <li><strong>All Products:</strong> <code>GET /api/products</code></li>
            <li><strong>Frontend Sync:</strong> Data automatically syncs to <code>data/content.json</code></li>
        </ul>
        <div class="mt-3">
            <a href="{{ url_for('api_products') }}" class="btn btn-outline-primary btn-sm" target="_blank">
                <i class="fas fa-code me-2"></i>View API Response
            </a>
            <a href="http://localhost:8000" class="btn btn-outline-success btn-sm" target="_blank">
                <i class="fas fa-external-link-alt me-2"></i>View Frontend
            </a>
        </div>
    </div>
</div>
{% endblock %}