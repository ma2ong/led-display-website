<!DOCTYPE html>
{% extends "base.html" %}

{% block title %}系统设置 - LED显示屏管理后台{% endblock %}
{% block page_title %}系统设置{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-cog me-2"></i>
                    网站基本设置
                </h5>
            </div>
            <div class="card-body">
                <form method="POST">
                    {% for setting in settings_list %}
                    <div class="mb-3">
                        <label for="{{ setting.key }}" class="form-label">
                            {{ setting.description or setting.key }}
                        </label>
                        {% if setting.key in ['company_address_en', 'company_address_zh'] %}
                            <textarea class="form-control" id="{{ setting.key }}" name="{{ setting.key }}" rows="3">{{ setting.value or '' }}</textarea>
                        {% else %}
                            <input type="text" class="form-control" id="{{ setting.key }}" name="{{ setting.key }}" 
                                   value="{{ setting.value or '' }}">
                        {% endif %}
                        {% if setting.key == 'site_title_en' %}
                            <div class="form-text">英文网站标题</div>
                        {% elif setting.key == 'site_title_zh' %}
                            <div class="form-text">中文网站标题</div>
                        {% elif setting.key == 'company_phone' %}
                            <div class="form-text">公司联系电话</div>
                        {% elif setting.key == 'company_email' %}
                            <div class="form-text">公司邮箱地址</div>
                        {% endif %}
                    </div>
                    {% endfor %}
                    
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> 保存设置
                        </button>
                        <button type="button" class="btn btn-secondary" onclick="location.reload()">
                            <i class="fas fa-undo"></i> 重置
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">
                <h6 class="mb-0"><i class="fas fa-info-circle me-2"></i>系统信息</h6>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <h6>服务状态：</h6>
                    <ul class="list-unstyled small">
                        <li><i class="fas fa-circle text-success me-2"></i>前端网站: 运行中</li>
                        <li><i class="fas fa-circle text-success me-2"></i>管理后台: 运行中</li>
                        <li><i class="fas fa-circle text-success me-2"></i>数据库: 正常</li>
                    </ul>
                </div>
                
                <div class="mb-3">
                    <h6>快速链接：</h6>
                    <div class="d-grid gap-2">
                        <a href="http://localhost:8000" target="_blank" class="btn btn-outline-primary btn-sm">
                            <i class="fas fa-external-link-alt me-2"></i>查看前端网站
                        </a>
                        <a href="{{ url_for('dashboard') }}" class="btn btn-outline-secondary btn-sm">
                            <i class="fas fa-tachometer-alt me-2"></i>返回仪表板
                        </a>
                    </div>
                </div>
                
                <div class="alert alert-info small">
                    <i class="fas fa-lightbulb"></i>
                    <strong>提示：</strong>修改设置后，前端网站将自动更新显示内容。
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}