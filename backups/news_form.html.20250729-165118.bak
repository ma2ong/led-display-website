<!DOCTYPE html>
{% extends "base.html" %}

{% block title %}{{ '编辑新闻' if action == 'edit' else '添加新闻' }} - LED显示屏管理后台{% endblock %}
{% block page_title %}{{ '编辑新闻' if action == 'edit' else '添加新闻' }}{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-newspaper me-2"></i>
                    {{ '编辑新闻信息' if action == 'edit' else '添加新闻' }}
                </h5>
            </div>
            <div class="card-body">
                <form method="POST" enctype="multipart/form-data">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="title_en" class="form-label">新闻标题 (英文) *</label>
                                <input type="text" class="form-control" id="title_en" name="title_en" 
                                       value="{{ news.title_en if news else '' }}" required>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="title_zh" class="form-label">新闻标题 (中文) *</label>
                                <input type="text" class="form-control" id="title_zh" name="title_zh" 
                                       value="{{ news.title_zh if news else '' }}" required>
                            </div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="category" class="form-label">新闻分类</label>
                                <select class="form-select" id="category" name="category">
                                    <option value="">选择分类</option>
                                    <option value="Technology" {{ 'selected' if news and news.category == 'Technology' else '' }}>技术</option>
                                    <option value="Industry" {{ 'selected' if news and news.category == 'Industry' else '' }}>行业</option>
                                    <option value="Company" {{ 'selected' if news and news.category == 'Company' else '' }}>公司</option>
                                    <option value="Product" {{ 'selected' if news and news.category == 'Product' else '' }}>产品</option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="status" class="form-label">发布状态</label>
                                <select class="form-select" id="status" name="status">
                                    <option value="published" {{ 'selected' if not news or news.status == 'published' else '' }}>发布</option>
                                    <option value="draft" {{ 'selected' if news and news.status == 'draft' else '' }}>草稿</option>
                                </select>
                            </div>
                        </div>
                    </div>
                    
                    <div class="mb-3">
                        <label for="content_en" class="form-label">新闻内容 (英文)</label>
                        <textarea class="form-control" id="content_en" name="content_en" rows="8">{{ news.content_en if news else '' }}</textarea>
                    </div>
                    
                    <div class="mb-3">
                        <label for="content_zh" class="form-label">新闻内容 (中文)</label>
                        <textarea class="form-control" id="content_zh" name="content_zh" rows="8">{{ news.content_zh if news else '' }}</textarea>
                    </div>
                    
                    <div class="mb-3">
                        <label for="news_image" class="form-label">新闻图片</label>
                        {% if news and news.image %}
                        <div class="mb-2">
                            <img src="{{ news.image }}" alt="当前新闻图片" class="img-thumbnail" style="max-width: 200px; max-height: 150px;">
                            <div class="form-text">当前图片</div>
                        </div>
                        {% endif %}
                        <input type="file" class="form-control" id="news_image" name="news_image" accept="image/*">
                        <div class="form-text">支持 PNG, JPG, JPEG, GIF, WEBP 格式，最大16MB</div>
                    </div>
                    
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> {{ '更新新闻' if action == 'edit' else '添加新闻' }}
                        </button>
                        <a href="{{ url_for('news') }}" class="btn btn-secondary">
                            <i class="fas fa-arrow-left"></i> 返回列表
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">
                <h6 class="mb-0"><i class="fas fa-info-circle me-2"></i>填写说明</h6>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <h6>新闻分类说明：</h6>
                    <ul class="list-unstyled small">
                        <li><strong>技术:</strong> 技术相关新闻</li>
                        <li><strong>行业:</strong> 行业动态和趋势</li>
                        <li><strong>公司:</strong> 公司新闻和公告</li>
                        <li><strong>产品:</strong> 产品发布和更新</li>
                    </ul>
                </div>
                
                <div class="alert alert-info small">
                    <i class="fas fa-lightbulb"></i>
                    <strong>提示：</strong>新闻内容将同时显示在中英文网站上，请确保内容准确完整。
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}